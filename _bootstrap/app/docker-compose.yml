version: '3'  
services:
  api:
    build: ./api
    volumes:
      - ./api:/var/www
    ports:
      - 8080:80
    command: composer require tcdent/php-restclient vlucas/phpdotenv && composer install
